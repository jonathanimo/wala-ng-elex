elexApp.controller('ResetCtrl', function($scope,$stateParams,Restangular,resetToken) {

  $scope.forgotPw = function() {
    
  }; //send email with request

  $scope.resetPw = function(){

  };// reset password if token is still valid
}); //StatusController